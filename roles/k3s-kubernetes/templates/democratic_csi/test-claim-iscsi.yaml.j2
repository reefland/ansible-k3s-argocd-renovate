kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: test-claim-iscsi
  annotations:
    volume.beta.kubernetes.io/storage-class: "{{install.democratic_csi.iscsi.provisioner}}-csi"
spec:
  storageClassName: {{install.democratic_csi.iscsi.provisioner}}-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
